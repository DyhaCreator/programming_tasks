#include <iostream>
#include <vector>
using namespace std;
using ll = long long;

const int INF = 1e9 + 7;

int main() {
    int n, p, q, r;
    cin >> n >> p >> q >> r;
    vector<int> a(n);
    for (auto &x : a)
        cin >> x;
    
    return 0;
}