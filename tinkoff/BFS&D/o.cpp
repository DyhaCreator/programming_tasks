#include <iostream>
#include <vector>
#define ll long long
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> c(n);
    for (auto &x : c)
        cin >> x;
    int m;
    cin >> m;
    
    return 0;
}