#include <iostream>
#include <vector>
#define ll long long
using namespace std;

int main() {
    int n, k, x;
    cin >> n >> k >> x;
    vector<int>a = vector<int>(n);
    for (auto &x : a)
        cin >> x;
    
    return 0;
}