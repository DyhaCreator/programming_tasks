#include <iostream>
#include <vector>
#define ll long long
using namespace std;

int main() {
    int t;
    cin >> t;
    for (int i = 0; i < t; i++) {
        int n, f, a, b;
        cin >> n >> f >> a >> b;
        vector<int>m = vector<int>(n);
        for (auto &x : m)
            cin >> x;
        
    }
    return 0;
}