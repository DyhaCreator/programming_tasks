#include <iostream>
#include <vector>
#include <string>
#define ll long long
using namespace std;

int main() {
    string str;
    cin >> str;
    int n = int(str.size());
    vector<string>ans = {};
    
    return 0;
}