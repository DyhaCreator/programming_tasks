#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {
    int h, w, x0, y0, x1, y1;
    cin >> h >> w >> y0 >> x0 >> y1 >> x1;
    if (y1 <= y0) {
        cout << "Draw" << endl;
    } else {
        if ((y1 - y0) % 2 == 0) {
            
        }
    }
}

int main() {
    int t;
    cin >> t;
    while (t--) solve();
    return 0;
}