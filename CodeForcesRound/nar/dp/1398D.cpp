#include <bits/stdc++.h>
using namespace std;
using ll = long long;

const int INF = 1e9 + 7;

int main() {
    int R, G, B;
    cin >> R >> G >> B;
    vector<int> r(R);
    vector<int> g(G);
    vector<int> b(B);
    for (auto &x : r)
        cin >> x;
    for (auto &x : g)
        cin >> x;
    for (auto &x : b)
        cin >> x;
    sort(r.rbegin(), r.rend());
    sort(g.rbegin(), g.rend());
    sort(b.rbegin(), b.rend());
    int a = 0;
    int b = 0;
    int c = 0;
    while ()
    return 0;
}