#include <iostream>
#include <vector>
using namespace std;
using ll = long long;

const int INF = 1e9 + 7;

void solve() {
    int a, b;
    cin >> a >> b;
    cout << (0 - a) + (b - 0) << endl;
}

int main() {
    int t;
    cin >> t;
    while (t--) solve();
    return 0;
}