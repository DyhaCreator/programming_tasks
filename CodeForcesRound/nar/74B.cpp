#include <iostream>
#include <vector>
#include <string>
#define ll long long
using namespace std;

int main() {
    int n, m, k;
    cin >> n >> m >> k;
    string str;
    cin >> str >> str;
    // cout << str << endl;
    bool flagC;
    if (str == "head") {
        flagC = true;
    } else {
        flagC = false;
    }
    cin >> str;
    int len = str.size();
    for (int i = 0; i < len; i++) {
        
    }
    return 0;
}